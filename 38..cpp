#include <bits/stdc++.h>
using namespace std;
int main(){queue<int>q;for(int i:{1,2,3})q.push(i);stack<int>s;while(!q.empty()){s.push(q.front());q.pop();}while(!s.empty()){cout<<s.top();s.pop();}}
